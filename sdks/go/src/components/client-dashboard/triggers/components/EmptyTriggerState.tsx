import { Card } from "@/components/ui/card";

export const EmptyTriggerState = () => {
  return (
    <div className="text-center p-4 text-muted-foreground">
      No triggers configured yet.
    </div>
  );
};